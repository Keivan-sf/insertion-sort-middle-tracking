fn main() {
    let mut elements: [i32; 5] = [1, 4, 0, -32, 10];
    let n = 5;
    for i in 1..5 {
        let key = elements[i];
        let mut j = i - 1;
        while key < elements[j] {
            elements[j + 1] = elements[j];
            if j == 0 {
                break;
            };
        }
    }
}
